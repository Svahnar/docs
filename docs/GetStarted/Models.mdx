---
title: "Models"
description: "Explore the large language models (LLMs) supported by SVAHNAR, including GPT-4o, GPT-5, Llama, and Deepseek."
keywords: ['ai models', 'llm', 'gpt-4o', 'gpt-5', 'llama', 'deepseek', 'qwen', 'svahnar models']
---

import styles from './customcss/modelList.module.css';

# Models & Pricing

Select the right level of intelligence for your agents from a diverse range of state-of-the-art AI models.

This guide outlines the available models, their credit costs, and how consumption is calculated in SVAHNAR.



---

## 💎 Available Models

The following models are available for immediate use in your agent configurations.

<div className="table-container">
  <table>
    <thead>
      <tr>
        <th>Provider</th>
        <th>Model Name (Config ID)</th>
        <th>Credits Per Run</th>
      </tr>
    </thead>
    <tbody>
      {/* OpenAI Models */}
      <tr>
        <td rowspan="7"><strong>OpenAI</strong></td>
        <td><code>gpt-4o-mini</code></td>
        <td>1</td>
      </tr>
      <tr>
        <td><code>gpt-4o</code></td>
        <td>15</td>
      </tr>
      <tr>
        <td><code>gpt-5-nano</code></td>
        <td>1</td>
      </tr>
      <tr>
        <td><code>gpt-5-mini</code></td>
        <td>3</td>
      </tr>
      <tr>
        <td><code>gpt-5</code></td>
        <td>15</td>
      </tr>
      <tr>
        <td><code>gpt-oss-120b</code></td>
        <td>1</td>
      </tr>
      <tr>
        <td><code>gpt-oss-20b</code></td>
        <td>1</td>
      </tr>

      {/* Meta Llama Models */}
      <tr>
        <td rowspan="3"><strong>Meta Llama</strong></td>
        <td><code>llama3-1-8b-instruct</code></td>
        <td>1</td>
      </tr>
      <tr>
        <td><code>llama3-3-70b-instruct</code></td>
        <td>2</td>
      </tr>
      <tr>
        <td><code>llama4-maverick-17b-instruct</code></td>
        <td>2</td>
      </tr>
      
      {/* Deepseek Models */}
      <tr>
        <td rowspan="2"><strong>Deepseek</strong></td>
        <td><code>deepseek.v3.2</code></td>
        <td>3</td>
      </tr>
      <tr>
        <td><code>deepseek.r1-v1:0</code></td>
        <td>7</td>
      </tr>

      {/* Qwen Models */}
      <tr>
        <td rowspan="5"><strong>Qwen</strong></td>
        <td><code>qwen3-32b</code></td>
        <td>1</td>
      </tr>
      <tr>
        <td><code>qwen3-coder-30b-a3b</code></td>
        <td>1</td>
      </tr>
      <tr>
        <td><code>qwen3-235b-a22b</code></td>
        <td>4</td>
      </tr>
      <tr>
        <td><code>qwen3-vl-235b-a22b</code></td>
        <td>4</td>
      </tr>
      <tr>
        <td><code>qwen3-coder-next</code></td>
        <td>4</td>
      </tr>
    </tbody>
  </table>
</div>

:::tip Enterprise
**Need custom solutions?** Enterprise customers can contact sales for access to fine-tuned models or custom deployments.
:::

---

## 📚 Practical Recipes

### Setting the Model in Config

To use a specific model, copy the **Model Name** from the table above and paste it into the `model` parameter of your YAML configuration.

> **Use Case:** Configuring an agent to use a cost-effective model for simple tasks.

```yaml showLineNumbers
create_agent_network:
  agent-1:
    agent_name: simple_task_agent
    LLM_config:
        params:
          # Paste the Model Name here
          model: gpt-4o-mini
    tools: []
    agent_function:
      - Reply to the user's greeting.
```

## 💡 Core Concepts

Before selecting a model, it is important to understand how SVAHNAR calculates usage.

### 1. What is a "Run"?
In a multi-agent network, pricing is calculated based on **Runs**. 
A **Run** occurs whenever an agent is triggered to perform a task or participate in a conversation.

### 2. How are credits consumed?
Credits are deducted strictly on a **pay-per-run** basis.
* If an agent uses **gpt-4o-mini**, 1 credit is deducted for that turn.
* If an agent uses **gpt-5**, 15 credits are deducted for that turn.

:::info
In a complex workflow where multiple agents interact, the total cost is the sum of the credits required for each agent's individual participation.
:::